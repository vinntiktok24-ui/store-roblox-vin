let cart=0;function addCart(name){cart++;document.getElementById("cartCount").textContent=cart;alert(name+" ditambahkan ke keranjang!")}function showCart(){alert(cart?`Keranjang: ${cart} item`:"Keranjang masih kosong.")}function scrollToItems(){document.getElementById("items").scrollIntoView({behavior:"smooth"})}function filterItems(type,btn){document.querySelectorAll(".categories button").forEach(b=>b.classList.remove("active"));btn.classList.add("active");document.querySelectorAll(".card").forEach(c=>{c.style.display=(type==="all"||c.dataset.type===type)?"block":"none"})}